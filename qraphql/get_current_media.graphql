query GetCurrentMedia($userId: Int, $status: MediaListStatus) {
  Page {
    mediaList(userId: $userId, status: $status) {
      media {
        id
        title {
          userPreferred
        }
        type
        episodes
        chapters
      }
      progress
    }
  }
}