query GetAnime($id: Int!) {
    Media(id: $id, type: ANIME) {
        id
        title {
            romaji
            english
            native
        }
        status
        season
        episodes
        coverImage {
            medium
        }
    }
}
